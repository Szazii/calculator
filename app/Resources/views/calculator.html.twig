{% extends 'base.html.twig' %}


{% block body %}
    <div style="text-align: center">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        <button class="siema" value="czesc eniu co tam" id="testowe">siema</button>

        {% for msg in app.session.flashBag.get('error') %}
            <div class="alert alert-danger">
                {{ msg }}
            </div>
        {% endfor %}
    </div>
{% endblock %}